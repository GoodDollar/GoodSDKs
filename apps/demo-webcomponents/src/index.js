// Import the component package.
// This executes the code in ui-components' main JS file,
// which should include the customElements.define call.
import "@goodsdks/ui-components"

// You don't need to do anything else here for the component
// to be usable in HTML, as long as it registers itself upon import.

console.log("Claim Button component should be registered now.")
